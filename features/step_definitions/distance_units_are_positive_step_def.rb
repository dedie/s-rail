When(/^a list of station distances are received$/) do
  pending # parse file with ruby code similar to response_parser.rb and extract distances as array
end

When(/^the distances are parsed$/) do
  pending # Iterate through array and return number of negative values
end

Then(/^no negative distances are found$/) do
  pending # Verify number of negative values are 0
end